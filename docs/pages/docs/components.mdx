import { Tabs } from 'nextra/components'

# Components

@loopcod/ui provides a comprehensive set of components for building SaaS applications.

## UI Primitives

### Button

Displays a button or a component that looks like a button.

```tsx
import { Button } from "@/components/ui/button"

<Button>Click me</Button>
<Button variant="outline">Outline</Button>
<Button variant="destructive">Delete</Button>
```

**Props**

| Prop | Type | Description |
|------|------|-------------|
| `variant` | `default` \| `destructive` \| `outline` \| `secondary` \| `ghost` \| `link` | Button style |
| `size` | `default` \| `sm` \| `lg` \| `icon` | Button size |
| `loading` | `boolean` | Show loading spinner |

---

### Card

A container component with header, content, and footer sections.

```tsx
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from "@/components/ui/card"

<Card>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card Description</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Card content</p>
  </CardContent>
  <CardFooter>
    <Button>Action</Button>
  </CardFooter>
</Card>
```

---

### Input

Form input with built-in error handling.

```tsx
import { Input } from "@/components/ui/input"

<Input placeholder="Email" type="email" />
<Input placeholder="Password" type="password" />
<Input error="Invalid email" />
```

---

## Auth Components

### LoginForm

Complete login form with email/password, validation, and error handling.

```tsx
import { LoginForm } from "@/components/auth/login-form"

export default function LoginPage() {
  return (
    <LoginForm 
      onSubmit={async (data) => {
        // Handle login
        console.log(data)
      }}
    />
  )
}
```

**Props**

| Prop | Type | Description |
|------|------|-------------|
| `onSubmit` | `(data: LoginData) => Promise<void>` | Form submission handler |
| `redirectUrl` | `string` | Where to redirect after login |
| `showForgotPassword` | `boolean` | Show forgot password link |
| `showSignUp` | `boolean` | Show signup link |

---

### SignupForm

User registration form with validation.

```tsx
import { SignupForm } from "@/components/auth/signup-form"

export default function SignupPage() {
  return (
    <SignupForm 
      onSubmit={async (data) => {
        // Handle signup
        console.log(data)
      }}
    />
  )
}
```

---

## Dashboard Components

### DashboardShell

Complete dashboard layout with sidebar, header, and navigation.

```tsx
import { DashboardShell } from "@/components/dashboard/shell"

export default function DashboardPage() {
  return (
    <DashboardShell>
      <h1>Dashboard Content</h1>
    </DashboardShell>
  )
}
```

---

### StatsCards

Display metric cards with trend indicators.

```tsx
import { StatsCards } from "@/components/dashboard/stats-cards"

<StatsCards 
  stats={[
    {
      title: "Revenue",
      value: "$45,231",
      icon: DollarSign,
      trend: { value: 12, label: "from last month", positive: true }
    },
    {
      title: "Users",
      value: "2,345",
      icon: Users,
      trend: { value: 8, label: "from last month", positive: true }
    }
  ]}
/>
```

---

## Billing Components

### PricingTable

Pricing plans comparison with features list.

```tsx
import { PricingTable } from "@/components/billing/pricing-table"

const tiers = [
  {
    name: "Starter",
    description: "For small projects",
    price: { monthly: 0, yearly: 0 },
    features: [
      { name: "Up to 3 projects", included: true },
      { name: "Basic analytics", included: true },
      { name: "Priority support", included: false },
    ],
    cta: "Get Started",
  },
  {
    name: "Pro",
    description: "For growing teams",
    price: { monthly: 29, yearly: 290 },
    features: [
      { name: "Unlimited projects", included: true },
      { name: "Advanced analytics", included: true },
      { name: "Priority support", included: true },
    ],
    cta: "Start Trial",
    popular: true,
  },
]

<PricingTable tiers={tiers} />
```

---

## Available Components

### UI Primitives

- `button` — Interactive button
- `card` — Container component
- `input` — Form input
- `label` — Form label
- `badge` — Status badge
- `avatar` — User avatar
- `checkbox` — Checkbox input
- `dialog` — Modal dialog
- `dropdown-menu` — Dropdown menu
- `sheet` — Slide-out panel
- `tabs` — Tabbed interface
- `toast` — Notification toast
- `separator` — Visual divider

### Auth

- `login-form` — Login form with validation
- `signup-form` — Signup form
- `forgot-password` — Password reset request
- `reset-password` — Password reset confirmation
- `oauth-buttons` — Social login buttons

### Dashboard

- `shell` — Dashboard layout
- `stats-cards` — Metric cards
- `data-table` — Advanced data table
- `nav` — Navigation component

### Billing

- `pricing-table` — Pricing comparison
- `checkout-form` — Payment form
- `subscription-manager` — Subscription management
